import { FC } from "react";

interface Props {
    onClose: () => void;
}

export const AddLauncherDialog: FC<Props> = () => {
    return (
        <div>
            Add Launcher Dialog
        </div>
    );
};
